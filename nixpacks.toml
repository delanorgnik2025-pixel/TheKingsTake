[variables]
NODE_OPTIONS = "--max-old-space-size=4096"
NPM_CONFIG_PRODUCTION = "false"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx vite build"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "npm run start:server"
